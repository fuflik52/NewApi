# 🚀 Cosmo Dashboard

Современная панель управления API с космическим дизайном, графиками и темной темой.

## 📋 Требования

Перед запуском убедитесь, что на вашем сервере установлены:

*   **Node.js** (версия 18.0.0 или новее)
*   **npm** (обычно устанавливается вместе с Node.js)

## 🛠 Установка

1.  **Скачайте файлы проекта** на ваш сервер.
2.  **Откройте терминал** в папке с проектом.
3.  **Установите зависимости** командой:

```bash
npm install
```

## 💻 Запуск в режиме разработки

Если вы хотите редактировать код и видеть изменения в реальном времени:

```bash
npm run dev
```

Сайт будет доступен по адресу: `http://localhost:5173`

## 🌍 Запуск на боевом сервере (Production)

Для полноценной работы (включая загрузку картинок через API) используйте встроенный сервер.

### Шаг 1: Сборка проекта

Выполните команду:

```bash
npm run build
```

Эта команда создаст папку `dist` в корне проекта.

### Шаг 2: Запуск сервера

Запустите сервер, который будет раздавать сайт и обрабатывать API-запросы (загрузку картинок):

```bash
npm start
```

Сервер запустится на порту **3000**.
Сайт будет доступен по адресу: `http://ваш-ip:3000` или `http://bublickrust.ru:3000`.

### Запуск в фоновом режиме (опционально)

Чтобы сервер работал постоянно (даже после закрытия консоли), используйте `pm2`:

```bash
npm install -g pm2
pm2 start server.js --name "cosmo-dashboard"
pm2 save
```

## ℹ️ Важное примечание

Этот проект использует гибридный подход:
*   **Frontend**: React приложение (Mock Database для статистики в localStorage).
*   **Backend**: Node.js сервер (`server.js`) для реальной обработки загрузки файлов через API (`/api/images/upload`).

Загруженные изображения сохраняются в папку `dist/uploads`.
